export const ArrayProp = {
  type: Array,
  default() {
    return [];
  }
};

export const NumberProp = {
  type: Number,
  default: 0
};

export const StringProp = {
  type: String,
  default: ''
};
